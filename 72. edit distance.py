class Solution:
    def minDistance(self, word1: str, word2: str) -> int:
        n = len(word1)
        m = len(word2)
        edits = 1

        while word1 != word2:
            pass
        return edits
    
print(Solution().minDistance("horse", "ros"))